# Python deps for Relay. Install with: pip install -r requirements.txt
#
# proxima is NOT here on purpose. It's my own HNSW engine, built locally from
# C:\Users\susha\New folder\proxima and pip-installed from that source, not PyPI.
# Check it with: python -c "import proxima"

fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2
pydantic-settings>=2
httpx>=0.27
pyyaml>=6
numpy>=1.26
fastembed>=0.3
openai>=1.0
anthropic>=0.25
locust>=2.0
